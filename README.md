# 🚀 Yousef sameer Instant Indexer

نظام متكامل لأرشفة الروابط فورياً في محرك بحث جوجل باستخدام **Google Indexing API** و **GitHub Actions**. يتميز النظام بالسرعة القصوى، التباين العالي للواجهة، والتكلفة الصفرية.

---

## 🌟 المميزات الرئيسية (Key Features)

* **Zero-Cost Hosting:** يعمل بالكامل على خوادم GitHub Actions المجانية.
* **Public Repo Optimized:** مصمم للمستودعات العامة لتجنب استهلاك دقائق الحساب الخاص (Actions Minutes).
* **Security First:** تشفير كامل لبيانات Google Service Account عبر نظام GitHub Secrets.
* **Auto-Cleanup:** تفريغ تلقائي لملف الروابط بعد نجاح الأرشفة للحفاظ على نظافة المستودع.
* **High Contrast UI:** واجهة مستخدم محسنة بصرياً لسهولة القراءة والعمل السريع.

---

## 📂 هيكل المشروع (Project Structure)



* `index.html`: لوحة التحكم لإدارة الإعدادات وإدخال الروابط.
* `indexer.py`: المحرك البرمجي (بايثون) للتواصل مع جوجل.
* `urls.txt`: الملف الوسيط لتخزين الروابط المطلوب أرشفتها.
* `.github/workflows/main.yml`: نظام الأتمتة الذي يشغل العملية عند كل تحديث.

---

## ⚙️ إعدادات التشغيل (Setup Guide)

### 1. إعداد Google Cloud
1.  قم بتفعيل **Indexing API** في [Google Cloud Console](https://console.cloud.google.com/).
2.  أنشئ **Service Account** وحمّل مفتاح الـ **JSON**.
3.  أضف إيميل الحساب (Service Account Email) كمالك (**Owner**) لموقعك في **Google Search Console**.

### 2. إعداد مستودع GitHub
1.  **Visibility:** اجعل المستودع **Public** لضمان التشغيل المجاني غير المحدود.
2.  **GitHub Secrets:** * اذهب إلى `Settings > Secrets and variables > Actions`.
    * أضف سر جديد باسم `GOOGLE_INDEXING_JSON` وضع بداخله محتوى ملف الـ JSON كاملاً.
3.  **Permissions:** * من `Settings > Actions > General` وفي قسم **Workflow permissions**، اختر **Read and write permissions** ثم اضغط حفظ.



---

## 🚀 طريقة الاستخدام (How to Use)

1.  افتح ملف `index.html` (سواء محلياً أو عبر GitHub Pages).
2.  أدخل الـ **GitHub Token** و **Repository Path**.
3.  ضع روابط المقالات (رابط واحد لكل سطر).
4.  اضغط على **"إرسال الروابط للأرشفة"**.
5.  تابع حالة التشغيل من تبويب **Actions** في مستودعك.

---

## 🛠️ استكشاف الأخطاء (Troubleshooting)

* **Error 403:** تأكد من تفعيل صلاحيات الكتابة (Write Permissions) في إعدادات Actions.
* **Billing Error:** تأكد أن المستودع **Public**؛ لأن المستودعات الخاصة تستهلك الدقائق المحدودة للحساب.
* **401 Unauthorized:** تأكد أن الـ Token المكتوب في واجهة الويب يمتلك صلاحية الوصول للمستودع (repo access).

---

## 👨‍💻 Author
**YOUSEF SAMEER**
*تطوير أدوات الذكاء الاصطناعي لخدمة المبدعين.*
